# MobileWeb 移动端 H5 需求方案

**项目**: EasyProject  
**模块**: MobileWeb (移动端 H5)  
**技术栈**: Vue 3 + TypeScript + Vant  
**创建时间**: 2026-03-24

---

## 📖 项目概述

### 1.1 项目定位
MobileWeb 是 EasyProject 项目的移动端 H5 应用，提供移动端的业务操作界面，主要面向 C 端用户。

### 1.2 核心功能
- 用户登录/注册
- 鲜花商城首页
- 商品浏览/详情
- 购物车管理
- 订单管理
- 地址管理
- 个人中心

---

## 🏗️ 架构设计

### 2.1 技术架构
```
MobileWeb/
├── src/
│   ├── api/               # API 请求封装
│   ├── assets/            # 静态资源
│   ├── components/        # 通用组件
│   ├── router/            # 路由配置
│   ├── store/             # 状态管理
│   ├── types/             # TypeScript 类型
│   ├── utils/             # 工具函数
│   ├── views/             # 页面组件
│   └── style/             # 全局样式
├── package.json           # 依赖配置
└── vite.config.js         # Vite 配置
```

### 2.2 技术栈
- **框架**: Vue 3.3
- **语言**: TypeScript 5.0
- **UI**: Vant 4.0
- **构建**: Vite 3.0
- **状态**: Pinia 2.1
- **路由**: Vue Router 4.1
- **HTTP**: Axios

---

## 📋 功能模块

### 3.1 用户模块
| 页面 | 路由 | 说明 |
|------|------|------|
| 登录页 | /login | 用户登录 |
| 注册页 | /register | 用户注册 |
| 个人中心 | /user | 个人信息 |

### 3.2 商城模块
| 页面 | 路由 | 说明 |
|------|------|------|
| 首页 | /home | 商城首页 |
| 分类页 | /category | 商品分类 |
| 商品详情 | /goods/:id | 商品详情 |
| 搜索页 | /search | 商品搜索 |

### 3.3 购物车模块
| 页面 | 路由 | 说明 |
|------|------|------|
| 购物车 | /cart | 购物车列表 |

### 3.4 订单模块
| 页面 | 路由 | 说明 |
|------|------|------|
| 订单列表 | /order/list | 订单列表 |
| 订单详情 | /order/:id | 订单详情 |
| 创建订单 | /order/create | 创建订单 |
| 订单支付 | /order/pay | 订单支付 |

### 3.5 地址模块
| 页面 | 路由 | 说明 |
|------|------|------|
| 地址列表 | /address/list | 地址列表 |
| 地址编辑 | /address/edit | 编辑地址 |
| 地址选择 | /address/select | 选择地址 |

---

## 🎨 UI 设计

### 4.1 布局结构
```
+------------------+
|    顶部导航栏     |
+------------------+
|                  |
|    主内容区       |
|                  |
+------------------+
|    底部标签栏     |
+------------------+
```

### 4.2 颜色规范
- **主色**: #1989fa (Vant 默认)
- **成功**: #07c160
- **警告**: #ff976a
- **危险**: #ee0a24
- **背景**: #f7f8fa

### 4.3 组件规范
- **按钮**: 统一使用 van-button
- **表单**: 统一使用 van-field
- **列表**: 统一使用 van-cell
- **弹窗**: 统一使用 van-dialog

---

## 🔧 技术实现

### 5.1 API 封装
```typescript
// http/request.ts
import axios from 'axios';

const request = axios.create({
  baseURL: 'http://localhost:7016',
  timeout: 30000,
});

// 请求拦截器
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器
request.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response.status === 401) {
      // 跳转登录
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### 5.2 路由配置
```typescript
// router/index.ts
const routes: RouteRecordRaw[] = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/user/Login.vue'),
  },
  {
    path: '/',
    name: 'home',
    component: () => import('@/views/home/Home.vue'),
  },
  {
    path: '/goods/:id',
    name: 'GoodsDetail',
    component: () => import('@/views/goods/Detail.vue'),
  },
];
```

### 5.3 状态管理
```typescript
// store/cart.ts
import { defineStore } from 'pinia';

export const useCartStore = defineStore('cart', {
  state: () => ({
    cartList: [],
    totalCount: 0,
  }),
  actions: {
    addToCart(product: any) {
      // 添加到购物车
    },
    removeFromCart(id: number) {
      // 从购物车移除
    },
    clearCart() {
      // 清空购物车
    },
  },
});
```

---

## 📊 页面统计

| 模块 | 页面数 | 组件数 | 代码行数 |
|------|--------|--------|----------|
| **用户模块** | 3 | 5 | ~800 |
| **商城模块** | 4 | 10 | ~1,500 |
| **购物车** | 1 | 3 | ~400 |
| **订单模块** | 4 | 8 | ~1,200 |
| **地址模块** | 3 | 5 | ~600 |
| **总计** | **15** | **31** | **~4,500** |

---

## 🎯 开发计划

### 阶段一：基础框架
- [ ] 项目初始化
- [ ] 路由配置
- [ ] 状态管理
- [ ] API 封装

### 阶段二：核心功能
- [ ] 登录/注册
- [ ] 商城首页
- [ ] 商品详情
- [ ] 购物车

### 阶段三：业务完善
- [ ] 订单管理
- [ ] 地址管理
- [ ] 个人中心

### 阶段四：优化完善
- [ ] 性能优化
- [ ] 单元测试
- [ ] 响应式适配
- [ ] 支付集成

---

## 📝 配置说明

### 开发环境配置
```typescript
// .env.development
VITE_API_BASE_URL=http://localhost:7016
VITE_APP_TITLE=鲜花商城
```

### 生产环境配置
```typescript
// .env.production
VITE_API_BASE_URL=https://api.yourdomain.com
VITE_APP_TITLE=鲜花商城
```

---

**文档版本**: v1.0  
**最后更新**: 2026-03-24  
**维护人**: 开发团队
