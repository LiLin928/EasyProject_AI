# PCWeb PC 管理后台需求方案

**项目**: EasyProject  
**模块**: PCWeb (PC 管理后台)  
**技术栈**: Vue 3 + TypeScript + Element Plus  
**创建时间**: 2026-03-24

---

## 📖 项目概述

### 1.1 项目定位
PCWeb 是 EasyProject 项目的 PC 端管理后台，提供完整的后台管理界面，支持工作流管理、ETL 调度、报表设计、大屏设计等功能。

### 1.2 核心功能
- 用户登录/权限管理
- 工作台/仪表盘
- 基础数据管理
- 业务工作流管理
- ETL 数据集成
- 报表管理
- 大屏设计
- 鲜花商城管理

---

## 🏗️ 架构设计

### 2.1 技术架构
```
PCWeb/
├── src/
│   ├── api/               # API 请求封装
│   ├── assets/            # 静态资源
│   ├── components/        # 通用组件
│   ├── composables/       # 组合式函数
│   ├── router/            # 路由配置
│   ├── store/             # 状态管理
│   ├── types/             # TypeScript 类型
│   ├── utils/             # 工具函数
│   └── views/             # 页面组件
├── package.json           # 依赖配置
└── vite.config.js         # Vite 配置
```

### 2.2 技术栈
- **框架**: Vue 3.3
- **语言**: TypeScript 5.0
- **UI**: Element Plus
- **构建**: Vite 3.0
- **状态**: Pinia 2.1
- **路由**: Vue Router 4.1
- **HTTP**: Axios

---

## 📋 功能模块

### 3.1 基础模块
| 页面 | 路由 | 说明 |
|------|------|------|
| 登录页 | /login | 用户登录 |
| 工作台 | /desktop | 工作台/仪表盘 |
| 个人中心 | /person | 个人信息管理 |

### 3.2 基础管理
| 页面 | 路由 | 说明 |
|------|------|------|
| 用户管理 | /basic/user | 用户 CRUD |
| 角色管理 | /basic/role | 角色权限 |
| 菜单管理 | /basic/menu | 菜单树管理 |
| 字典管理 | /basic/dic | 数据字典 |

### 3.3 工作流管理
| 页面 | 路由 | 说明 |
|------|------|------|
| 工作流列表 | /workflow/list | 工作流管理 |
| 工作流设计 | /workflow/design | 可视化设计器 |
| 实例监控 | /workflow/instances | 实例列表 |
| 我的待办 | /workflow/todos | 待办事项 |
| 我的已办 | /workflow/done | 已办事项 |

### 3.4 ETL 管理
| 页面 | 路由 | 说明 |
|------|------|------|
| ETL 任务 | /etl/task | ETL 任务列表 |
| ETL 设计 | /etl/design | ETL 设计器 |
| 实例监控 | /etl/instances | 执行监控 |

### 3.5 报表管理
| 页面 | 路由 | 说明 |
|------|------|------|
| 报表列表 | /report/list | 报表管理 |
| 报表设计 | /report/design | 报表设计器 |

### 3.6 大屏管理
| 页面 | 路由 | 说明 |
|------|------|------|
| 大屏列表 | /screen/project | 大屏项目 |
| 大屏设计 | /screen/design | 大屏设计器 |

### 3.7 鲜花商城
| 页面 | 路由 | 说明 |
|------|------|------|
| 商品管理 | /mall/goods | 商品管理 |
| 分类管理 | /mall/category | 分类管理 |
| 订单管理 | /mall/order | 订单管理 |
| 轮播图 | /mall/banner | 轮播图管理 |

---

## 🎨 UI 设计

### 4.1 布局结构
```
+------------------+
|    顶部导航栏     |
+------------------+
| 侧 |            |
| 边 |   主内容区   |
| 菜 |            |
| 单 |            |
+----+------------+
```

### 4.2 颜色规范
- **主色**: #409EFF (Element Plus 默认)
- **成功**: #67C23A
- **警告**: #E6A23C
- **危险**: #F56C6C
- **背景**: #F5F7FA

### 4.3 组件规范
- **按钮**: 统一使用 el-button
- **表单**: 统一使用 el-form
- **表格**: 统一使用 el-table
- **对话框**: 统一使用 el-dialog

---

## 🔧 技术实现

### 5.1 API 封装
```typescript
// http/request.ts
import axios from 'axios';

const request = axios.create({
  baseURL: 'http://localhost:7016',
  timeout: 30000,
});

// 请求拦截器
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器
request.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response.status === 401) {
      // 跳转登录
    }
    return Promise.reject(error);
  }
);
```

### 5.2 路由配置
```typescript
// router/index.ts
const routes: RouteRecordRaw[] = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/index/LoginPage.vue'),
  },
  {
    path: '/',
    name: 'home',
    component: () => import('@/views/index/RootPage.vue'),
    children: [
      {
        path: 'desktop',
        name: 'Desktop',
        component: () => import('@/views/index/DeskTop.vue'),
      },
      // ...其他路由
    ],
  },
];
```

### 5.3 状态管理
```typescript
// store/user.ts
import { defineStore } from 'pinia';

export const useUserStore = defineStore('user', {
  state: () => ({
    token: '',
    userInfo: null,
  }),
  actions: {
    setToken(token: string) {
      this.token = token;
      localStorage.setItem('token', token);
    },
    logout() {
      this.token = '';
      this.userInfo = null;
      localStorage.removeItem('token');
    },
  },
});
```

---

## 📊 页面统计

| 模块 | 页面数 | 组件数 | 代码行数 |
|------|--------|--------|----------|
| **基础模块** | 3 | 5 | ~1,000 |
| **基础管理** | 5 | 10 | ~2,000 |
| **工作流** | 5 | 15 | ~3,000 |
| **ETL** | 3 | 10 | ~2,000 |
| **报表** | 2 | 5 | ~1,000 |
| **大屏** | 2 | 8 | ~1,500 |
| **鲜花商城** | 4 | 10 | ~2,000 |
| **总计** | **24** | **63** | **~12,500** |

---

## 🎯 开发计划

### 阶段一：基础框架 (已完成)
- [ ] 项目初始化
- [ ] 路由配置
- [ ] 状态管理
- [ ] API 封装

### 阶段二：基础功能 (已完成)
- [ ] 登录页面
- [ ] 工作台
- [ ] 基础管理页面

### 阶段三：业务模块 (已完成)
- [ ] 工作流管理
- [ ] ETL 管理
- [ ] 报表管理
- [ ] 大屏管理
- [ ] 鲜花商城

### 阶段四：优化完善 (进行中)
- [ ] 性能优化
- [ ] 单元测试
- [ ] 响应式适配
- [ ] 国际化

---

## 📝 配置说明

### 开发环境配置
```typescript
// .env.development
VITE_API_BASE_URL=http://localhost:7016
VITE_APP_TITLE=EasyProject 管理后台
```

### 生产环境配置
```typescript
// .env.production
VITE_API_BASE_URL=https://api.yourdomain.com
VITE_APP_TITLE=EasyProject 管理后台
```

---

**文档版本**: v1.0  
**最后更新**: 2026-03-24  
**维护人**: 开发团队
