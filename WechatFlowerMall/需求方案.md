# 微信小程序鲜花商城需求方案

**项目**: EasyProject  
**模块**: guigu-flower-mall (微信小程序)  
**技术栈**: 微信小程序原生开发  
**创建时间**: 2026-03-24

---

## 📖 项目概述

### 1.1 项目定位
微信小程序鲜花商城，提供便捷的微信端鲜花购买体验，与 PC 管理后台和移动端 H5 共享后端 API。

### 1.2 核心功能
- 微信登录授权
- 鲜花商城首页
- 商品浏览/详情
- 购物车管理
- 订单管理
- 地址管理
- 微信支付
- 个人中心

---

## 🏗️ 架构设计

### 2.1 项目结构
```
guigu-flower-mall/
├── pages/                 # 页面目录
│   ├── index/            # 首页
│   ├── classify/         # 分类页
│   ├── shopping/         # 购物车
│   ├── my/               # 个人中心
│   ├── details/          # 商品详情
│   ├── pay/              # 支付页
│   └── order/            # 订单相关
├── components/            # 通用组件
├── static/                # 静态资源
├── store/                 # 状态管理
├── utils/                 # 工具函数
├── app.js                 # 小程序入口
├── app.json               # 小程序配置
└── project.config.json    # 项目配置
```

### 2.2 技术栈
- **框架**: 微信小程序原生
- **语言**: JavaScript ES6+
- **UI**: Vant Weapp
- **状态**: MobX-miniprogram
- **HTTP**: wx.request 封装

---

## 📋 功能模块

### 3.1 页面结构
| 页面 | 路径 | 说明 |
|------|------|------|
| 首页 | /pages/index/index | 商城首页 |
| 分类 | /pages/classify/classify | 商品分类 |
| 购物车 | /pages/shopping/shopping | 购物车 |
| 个人中心 | /pages/my/my | 个人中心 |
| 商品详情 | /pages/details/details | 商品详情 |
| 订单列表 | /pages/order/list | 订单列表 |
| 订单详情 | /pages/order/detail | 订单详情 |
| 支付页 | /pages/pay/pay | 订单支付 |
| 地址管理 | /pages/address/list | 地址管理 |

### 3.2 核心功能
| 功能 | 说明 |
|------|------|
| 微信登录 | 微信授权登录 |
| 商品展示 | 列表/详情/搜索 |
| 购物车 | 添加/删除/结算 |
| 订单 | 创建/支付/退款 |
| 地址 | 增删改查 |
| 支付 | 微信支付 |
| 物流 | 物流查询 |

---

## 🎨 UI 设计

### 4.1 底部标签栏
```json
{
  "tabBar": {
    "color": "#999999",
    "selectedColor": "#1989fa",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "首页",
        "iconPath": "static/tabs/home.png",
        "selectedIconPath": "static/tabs/home-active.png"
      },
      {
        "pagePath": "pages/classify/classify",
        "text": "分类",
        "iconPath": "static/tabs/category.png",
        "selectedIconPath": "static/tabs/category-active.png"
      },
      {
        "pagePath": "pages/shopping/shopping",
        "text": "购物车",
        "iconPath": "static/tabs/cart.png",
        "selectedIconPath": "static/tabs/cart-active.png"
      },
      {
        "pagePath": "pages/my/my",
        "text": "我的",
        "iconPath": "static/tabs/user.png",
        "selectedIconPath": "static/tabs/user-active.png"
      }
    ]
  }
}
```

### 4.2 颜色规范
- **主色**: #1989fa
- **成功**: #07c160
- **警告**: #ff976a
- **危险**: #ee0a24
- **文字**: #323233
- **背景**: #f7f8fa

---

## 🔧 技术实现

### 5.1 网络请求封装
```javascript
// utils/request.js
const BASE_URL = 'https://api.yourdomain.com';

function request(url, data = {}, method = 'GET') {
  return new Promise((resolve, reject) => {
    const token = wx.getStorageSync('token');
    
    wx.request({
      url: BASE_URL + url,
      data,
      method,
      header: {
        'Content-Type': 'application/json',
        'Authorization': token ? `Bearer ${token}` : '',
      },
      success: (res) => {
        if (res.statusCode === 200) {
          resolve(res.data);
        } else if (res.statusCode === 401) {
          // 跳转登录
          wx.navigateTo({ url: '/pages/login/login' });
          reject(new Error('未登录'));
        } else {
          wx.showToast({ title: '请求失败', icon: 'none' });
          reject(new Error(res.data.message));
        }
      },
      fail: (err) => {
        wx.showToast({ title: '网络错误', icon: 'none' });
        reject(err);
      },
    });
  });
}

module.exports = {
  get: (url, data) => request(url, data, 'GET'),
  post: (url, data) => request(url, data, 'POST'),
};
```

### 5.2 状态管理
```javascript
// store/cart.js
import { observable, action } from 'mobx-miniprogram';

export const cartStore = observable({
  cartList: [],
  
  get totalCount() {
    return this.cartList.reduce((sum, item) => sum + item.count, 0);
  },
  
  get totalPrice() {
    return this.cartList.reduce((sum, item) => sum + item.price * item.count, 0);
  },
  
  addToCart: action(function (product) {
    const existItem = this.cartList.find(item => item.id === product.id);
    if (existItem) {
      existItem.count++;
    } else {
      this.cartList.push({ ...product, count: 1 });
    }
  }),
  
  removeFromCart: action(function (id) {
    this.cartList = this.cartList.filter(item => item.id !== id);
  }),
  
  clearCart: action(function () {
    this.cartList = [];
  }),
});
```

### 5.3 微信登录
```javascript
// pages/login/login.js
Page({
  data: {},
  
  async onLogin() {
    try {
      // 获取 code
      const { code } = await wx.login();
      
      // 调用后端 API 换取 token
      const res = await wx.request({
        url: 'https://api.yourdomain.com/api/Auth/wxLogin',
        method: 'POST',
        data: { code },
      });
      
      // 保存 token
      wx.setStorageSync('token', res.data.data.token);
      wx.setStorageSync('userInfo', res.data.data.userInfo);
      
      // 跳转首页
      wx.switchTab({ url: '/pages/index/index' });
    } catch (error) {
      wx.showToast({ title: '登录失败', icon: 'none' });
    }
  },
});
```

---

## 📊 页面统计

| 模块 | 页面数 | 组件数 | 代码行数 |
|------|--------|--------|----------|
| **基础页面** | 4 | 5 | ~1,000 |
| **商品模块** | 3 | 8 | ~1,200 |
| **购物车** | 1 | 3 | ~400 |
| **订单模块** | 3 | 6 | ~1,000 |
| **地址模块** | 2 | 3 | ~400 |
| **总计** | **13** | **25** | **~4,000** |

---

## 🎯 开发计划

### 阶段一：基础框架 (已完成)
- [ ] 项目初始化
- [ ] 底部标签栏
- [ ] 网络请求封装
- [ ] 状态管理

### 阶段二：核心功能 (已完成)
- [ ] 微信登录
- [ ] 商城首页
- [ ] 商品详情
- [ ] 购物车

### 阶段三：业务完善 (已完成)
- [ ] 订单管理
- [ ] 地址管理
- [ ] 微信支付
- [ ] 个人中心

### 阶段四：优化完善 (进行中)
- [ ] 性能优化
- [ ] 用户体验优化
- [ ] 物流查询
- [ ] 客服功能

---

## 📝 配置说明

### 项目配置
```json
{
  "appid": "wx1234567890abcdef",
  "projectname": "guigu-flower-mall",
  "setting": {
    "es6": true,
    "minified": true,
    "postcss": true
  }
}
```

### 服务器域名配置
```json
{
  "requestValidDomain": [
    "https://api.yourdomain.com"
  ]
}
```

---

**文档版本**: v1.0  
**最后更新**: 2026-03-24  
**维护人**: 开发团队
